<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.empserver.mapper.EmployeeDetailMapper">
    <resultMap id="employeeDetailResultMap" type="EmployeeDetail">
        <id property="employeeNo" column="emp_no"/>
        <result property = "birthDate" column="birth_date"/>
        <result property = "firstName" column="first_name"/>
        <result property = "lastName" column="last_name"/>
        <result property = "gender" column="gender"/>
        <result property = "hireDate" column="hire_date"/>
        <association property="title" javaType="Title">
            <id property="employeeNo" column="emp_no"/>
            <result property="title" column="title"/>
            <result property="fromDate" column="from_date"/>
            <result property="toDate" column="to_date"/>
        </association>
        <collection property="salaries" ofType="Salary" resultMap="SalaryResultMap" >
        </collection>
    </resultMap>
    <resultMap id="SalaryResultMap" type="Salary">
        <result property="employeeNo" column="salaries.emp_no"/>
        <result property="fromDate" column="from_date"/>
        <result property="salary" column="salary"/>
        <result property="toDate" column="to_date"/>
    </resultMap>
    <select id="selectById" parameterType="long" resultMap="employeeDetailResultMap">
                select E.emp_no,
                       E.birth_date,
                       E.first_name,
                       E.last_name,
                       E.gender,
                       E.hire_date,
                       T.emp_no,
                       T.title,
                       T.from_date,
                       T.to_date,
                       S.emp_no,
                       S.salary,
                       S.from_date,
                       S.to_date
                from employees E
                left outer join titles T on E.emp_no = T.emp_no
                left outer join salaries S on T.emp_no = S.emp_no
                where E.emp_no = #{id}
    </select>
</mapper>